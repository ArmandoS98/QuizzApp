<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_ranking">

    <fragment
        android:id="@+id/nav_ranking"
        android:name="com.tekun.quizzapp.view.RankingFragment"
        android:label="Ranking"
        tools:layout="@layout/fragment_ranking" />
    <fragment
        android:id="@+id/nav_quizzes"
        android:name="com.tekun.quizzapp.view.QuizzesFragment"
        android:label="Quizzes"
        tools:layout="@layout/fragment_quizzes" />
    <fragment
        android:id="@+id/nav_quiz"
        android:name="com.tekun.quizzapp.view.QuizzFragment"
        android:label="Quiz"
        tools:layout="@layout/fragment_quizz" />
</navigation>